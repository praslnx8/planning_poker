name: planning_poker
description: Planning Poker Tool for Agile Team.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^1.10.6
  firebase_database: ^9.0.4
  flutter_dotenv: ^5.0.2
  logger: ^1.0.0
  firebase_auth: ^3.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.17.12

flutter:
  uses-material-design: true

  assets:
    - env
